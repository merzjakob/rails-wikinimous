<!-- <h1>Articles#index</h1>
<p>Find me in app/views/articles/index.html.erb</p>
 -->
 <nav class="navbar navbar-default" role="navigation">
   <div class="navbar-header">
     <a class="navbar-logo" href="/">Wikinimous</a>
   </div>
 </nav>

<ul>
  <% @articles.each do |article|  %>
    <li>
      <h3> <%= link_to article.title, article_path(article) %> </h3> <br>
      <p> <%= article.content %> </p>
      <p><%= link_to 'remove article', article_path(article), method: :delete, data: {confirm: 'Are you sure?'} %></p>
    </li>
   <% end %>
</ul>

<h3><%=  link_to 'Add new article', new_article_path%></h3>
